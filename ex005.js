// Listas

let numeros = [1, 2, 3, 4, 5]   

console.log(numeros)
console.log(numeros[0]) // Acessando o primeiro elemento da lista
console.log(numeros[2]) // Acessando o terceiro elemento da lista
console.log(numeros.length) // Tamanho da lista
console.log(numeros[numeros.length - 1]) // Acessando o último elemento da lista

const frutas = ['maçã', 'banana', 'laranja', 'uva']

console.log(frutas)

frutas.push('abacaxi') // Adicionando um elemento no final da lista
console.log(frutas)

frutas.pop() // Removendo o último elemento da lista
console.log(frutas)